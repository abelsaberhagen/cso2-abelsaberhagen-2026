Estimated threshold time: 15000

The threshold was chosen because we were trying to measure 10000 exceptions and collected roughly 9000. 

There is a context switch because some points are very high compared to other points, meaning the OS was likely running 
another program for 4 x 10^5 nanoseconds.